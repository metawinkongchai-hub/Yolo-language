# โค้ดเทรน model 
from ultralytics import YOLO 

# ------------------------------------------------------- 
# 1. โหลดโมเดลเริ่มต้น (เลือกขนาดเล็กสุดเพื่อเทรนเร็ว)
# ------------------------------------------------------- 

model = YOLO("yolov8n.pt") # n = nano (เล็กสุดและเร็วสุด) 

# ------------------------------------------------------- 
#  2. เทรนโมเดลแบบเร็ว (ลดจำนวนรอบและ batch) 
# ------------------------------------------------------- 

model.train(data=r"C:\Users\Gigabyte\OneDrive\เดสก์ท็อป\CODE\backend\dataset\data.yaml", # path ไปยัง data.yaml 
            epochs=10, # 🔹 จากลดเหลือ 10 รอบ (เทรนเร็วขึ้นมาก) 
            imgsz=640, # 🔹 ลดขนาดภาพ (640 → 416) เร็วขึ้นอีก 
            batch=4, # 🔹 batch เล็กลง ใช้แรมน้อย 
            name="hand_pose_fasttrain", # 🔹 ตั้งชื่อผลลัพธ์ 
            workers=1, # 🔹 ลด threads ให้เหมาะกับ Windows 
            device='cpu', # 🔹 ถ้าไม่มี GPU ให้ใส่ 'cpu' (ถ้ามี GPU ใส่ device=0) 
) 

# ------------------------------------------------------- 
# 3. หลังเทรนเสร็จ โมเดลจะอยู่ที่ runs/detect/hand_pose_fasttrain/weights/best.pt 
# ------------------------------------------------------- 

print("\n✅ เทรนจบแล้ว! โมเดลอยู่ที่: runs/detect/hand_pose_fasttrain/weights/best.pt")